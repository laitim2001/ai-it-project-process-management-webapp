# IT Project Management Platform - 專案根目錄

## 📋 專案概述

這是一個使用 **T3 Stack** (Next.js + tRPC + Prisma + TypeScript) 構建的生產級 IT 專案流程管理平台。

## 🏗️ 根目錄結構

```
/
├── apps/                       # 應用程式
│   └── web/                   # Next.js 前端應用
│       ├── src/
│       │   ├── app/           # Next.js App Router（18 個頁面）
│       │   ├── components/    # React 組件（46 個）
│       │   ├── lib/           # 工具函數和 tRPC Client
│       │   ├── hooks/         # 自定義 Hooks
│       │   ├── i18n/          # 國際化配置
│       │   └── messages/      # 翻譯檔案
│       └── e2e/               # Playwright E2E 測試
│
├── packages/                   # 共享套件
│   ├── api/                   # tRPC API Layer（14 個 Routers）
│   │   ├── src/routers/      # 業務邏輯核心
│   │   └── src/lib/          # Email Service 等
│   ├── db/                    # Prisma Database Layer
│   │   └── prisma/           # Schema + Migrations
│   ├── auth/                  # NextAuth.js + Azure AD B2C
│   ├── tsconfig/             # 共享 TypeScript 配置
│   └── eslint-config/        # 共享 ESLint 配置
│
├── scripts/                    # 開發工具腳本
│   ├── check-environment.js   # 環境檢查（404 行）
│   └── check-index-sync.js    # 索引維護工具
│
├── docs/                       # 專案文檔
├── claudedocs/                 # AI 生成文檔和分析
│
├── .nvmrc                      # Node.js 版本: 20.11.0
├── package.json                # Workspace 根配置
├── turbo.json                  # Turborepo 配置
├── docker-compose.yml          # 本地服務（PostgreSQL, Redis, Mailhog）
└── .env                        # 環境變數
```

## 🎯 架構分層

### 1. 資料層（Database Layer）
- **位置**: `packages/db/prisma/`
- **技術**: Prisma + PostgreSQL
- **模式**: [查看 packages/db/prisma/.claude.md]

### 2. 業務邏輯層（API Layer）
- **位置**: `packages/api/src/routers/`
- **技術**: tRPC + Zod
- **模式**: [查看 packages/api/src/routers/.claude.md]

### 3. 認證層（Auth Layer）
- **位置**: `packages/auth/src/`
- **技術**: NextAuth.js + Azure AD B2C
- **模式**: [查看 packages/auth/src/.claude.md]

### 4. 前端層（Frontend Layer）
- **路由**: `apps/web/src/app/[locale]/`
- **組件**: `apps/web/src/components/`
- **工具**: `apps/web/src/lib/`
- **模式**: [查看對應的 .claude.md]

### 5. 測試層（Testing Layer）
- **E2E**: `apps/web/e2e/`
- **技術**: Playwright
- **模式**: [查看 apps/web/e2e/.claude.md]

## 🚀 快速開始

```bash
# 1. 安裝依賴
pnpm install

# 2. 環境變數
cp .env.example .env
# 編輯 .env 填入必要資訊

# 3. 啟動本地服務
docker-compose up -d

# 4. 資料庫遷移
pnpm db:migrate
pnpm db:generate

# 5. 環境檢查
pnpm check:env

# 6. 啟動開發服務器
pnpm dev
```

## 📚 關鍵文件索引

每個關鍵目錄都有 `.claude.md` 文件記錄模式和約定：

1. **packages/api/src/routers/.claude.md** - API Router 模式
2. **packages/db/prisma/.claude.md** - 資料模型模式
3. **packages/auth/src/.claude.md** - 認證模式
4. **apps/web/src/app/[locale]/.claude.md** - 頁面路由模式
5. **apps/web/src/components/.claude.md** - 組件開發模式
6. **apps/web/src/lib/.claude.md** - 工具函數模式
7. **apps/web/src/i18n/.claude.md** - 國際化模式
8. **apps/web/src/messages/.claude.md** - 翻譯資源模式
9. **apps/web/e2e/.claude.md** - E2E 測試模式
10. **apps/web/src/components/ui/.claude.md** - UI 組件模式
11. **apps/web/src/components/layout/.claude.md** - 佈局模式
12. **apps/web/src/hooks/.claude.md** - Custom Hooks 模式

## 🎯 核心技術棧

- **Framework**: Next.js 14.1.0 (App Router)
- **API**: tRPC 10.x (端對端類型安全)
- **Database**: Prisma 5.22.0 + PostgreSQL 16
- **Auth**: NextAuth.js + Azure AD B2C
- **UI**: shadcn/ui + Radix UI + Tailwind CSS
- **i18n**: next-intl（繁中/英文）
- **Testing**: Playwright (E2E)
- **Monorepo**: Turborepo + pnpm

## 🔗 資料流向

```
User Request
    ↓
Next.js App Router (apps/web/src/app/[locale]/)
    ↓
React Component (apps/web/src/components/)
    ↓
tRPC Client (apps/web/src/lib/trpc.ts)
    ↓
tRPC API Route (/api/trpc/[trpc])
    ↓
tRPC Router (packages/api/src/routers/)
    ↓
Prisma Client (packages/db/)
    ↓
PostgreSQL Database
```

## ⚠️ 全域重要約定

1. **所有業務邏輯必須在 API Layer**（不可在前端）
2. **所有資料模型在 Prisma Schema**（單一真相來源）
3. **禁止硬編碼文字**（必須使用 i18n）
4. **所有頁面必須使用 DashboardLayout**（除了 auth）
5. **tRPC 查詢只能在 Client Component**
6. **所有組件必須有 TypeScript 類型**
7. **檔案必須有 JSDoc 文檔**（156/156 已完成）

## 📖 開發指南

### 新增功能流程
1. 在 `packages/db/prisma/schema.prisma` 定義資料模型
2. 在 `packages/api/src/routers/` 創建 API Router
3. 在 `apps/web/src/app/[locale]/` 創建頁面
4. 在 `apps/web/src/components/` 創建組件
5. 在 `apps/web/src/messages/` 添加翻譯
6. 在 `apps/web/e2e/` 添加測試

### 除錯技巧
- **API 錯誤**: 檢查 tRPC Router 和 Prisma Query
- **UI 問題**: 檢查 React Component 和 Tailwind CSS
- **認證問題**: 檢查 NextAuth Session 和 Middleware
- **i18n 問題**: 運行 `pnpm validate:i18n`
- **環境問題**: 運行 `pnpm check:env`

## 📊 專案狀態

- **MVP**: 100% 完成（Epic 1-8）
- **代碼量**: ~30,000+ 行核心代碼
- **頁面數**: 18 個全功能頁面
- **組件數**: 46 個（26 UI + 20 業務）
- **API**: 14 個 tRPC Routers
- **測試**: 3 個核心工作流 E2E 測試
- **文檔**: 156/156 JSDoc 覆蓋率 100%

## 🔍 相關文件

- `CLAUDE.md` - AI 助手完整指南
- `README.md` - 專案概述
- `DEVELOPMENT-SETUP.md` - 跨平台設置指南（711 行）
- `PROJECT-INDEX.md` - 完整檔案索引（290+ 文件）
- `docs/` - 完整專案文檔
- `claudedocs/` - AI 生成的分析和規劃

## 💡 尋求幫助

- **環境問題**: 查看 `DEVELOPMENT-SETUP.md`
- **架構問題**: 查看 `docs/fullstack-architecture/`
- **模式問題**: 查看對應目錄的 `.claude.md`
- **開發歷史**: 查看 `DEVELOPMENT-LOG.md`
- **Bug 記錄**: 查看 `FIXLOG.md`
