{
  "name": "@itpm/auth",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "TypeScript type augmentations for NextAuth.js Session/JWT types. No runtime code - see apps/web/src/auth.ts for the actual NextAuth config.",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next-auth": "5.0.0-beta.30"
  },
  "devDependencies": {
    "@itpm/tsconfig": "workspace:*",
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
}
