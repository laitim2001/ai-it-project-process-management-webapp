# ==============================================================================
# IT 專案流程管理平台 - 環境變數（本地開發）
# ==============================================================================

# ------------------------------------------------------------------------------
# 環境設定
# ------------------------------------------------------------------------------
NODE_ENV=development
APP_NAME="IT Project Process Management Platform"
APP_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# 資料庫 (使用 Docker Compose 本地 PostgreSQL)
# ------------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres:localdev123@localhost:5434/itpm_dev"

# ------------------------------------------------------------------------------
# NextAuth.js 認證
# ------------------------------------------------------------------------------
# 臨時密鑰（本地開發用）- 生產環境請使用 openssl rand -base64 32 生成
NEXTAUTH_SECRET="dev-secret-key-change-in-production-use-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SESSION_MAX_AGE=86400

# ------------------------------------------------------------------------------
# Azure AD B2C（本地開發可選）
# ------------------------------------------------------------------------------
# 如果需要測試 Azure AD B2C，請填寫以下資訊
# AZURE_AD_B2C_TENANT_NAME="yourtenantname"
# AZURE_AD_B2C_CLIENT_ID="your-client-id"
# AZURE_AD_B2C_CLIENT_SECRET="your-client-secret"
# AZURE_AD_B2C_PRIMARY_USER_FLOW="B2C_1_signupsignin"

# ------------------------------------------------------------------------------
# Email 服務（本地開發使用 Mailhog）
# ------------------------------------------------------------------------------
# Mailhog SMTP 設定（Docker Compose 會啟動）
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=""
SMTP_PASSWORD=""

# SendGrid（生產環境用，本地可不填）
# SENDGRID_API_KEY="your-sendgrid-api-key"
SENDGRID_FROM_EMAIL="noreply@itpm.local"
SENDGRID_FROM_NAME="IT Project Management"

# ------------------------------------------------------------------------------
# Redis（Docker Compose 會啟動）
# ------------------------------------------------------------------------------
REDIS_URL="redis://localhost:6381"

# ------------------------------------------------------------------------------
# 前端相關
# ------------------------------------------------------------------------------
NEXT_PUBLIC_APP_NAME="IT Project Process Management Platform"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NEXT_PUBLIC_ENVIRONMENT="development"

# Feature Flags
NEXT_PUBLIC_FEATURE_AI_ASSISTANT=false
NEXT_PUBLIC_FEATURE_EXTERNAL_INTEGRATION=false

# ------------------------------------------------------------------------------
# 開發工具
# ------------------------------------------------------------------------------
LOG_LEVEL=info
LOG_FORMAT=pretty
DEBUG=false
VERBOSE_LOGGING=false

# ------------------------------------------------------------------------------
# 其他設定
# ------------------------------------------------------------------------------
TZ=Asia/Taipei
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
MAX_FILE_SIZE_MB=10
ALLOWED_FILE_TYPES=".pdf,.docx,.xlsx,.png,.jpg"

# ------------------------------------------------------------------------------
# 測試環境
# ------------------------------------------------------------------------------
TEST_DATABASE_URL="postgresql://postgres:localdev123@localhost:5434/itpm_test"
PLAYWRIGHT_TEST_BASE_URL="http://localhost:3000"
